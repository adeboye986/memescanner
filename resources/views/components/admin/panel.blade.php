@props(['title', 'eyebrow' => null, 'action' => null])
<section {{ $attributes->class('overflow-hidden rounded-2xl border border-slate-700/70 bg-slate-900/75 shadow-[0_18px_50px_rgba(0,0,0,0.16)]') }}><div class="flex items-center justify-between gap-5 border-b border-slate-800/90 bg-slate-900/35 px-5 py-4 sm:px-6"><div>@if($eyebrow)<p class="text-xs font-bold uppercase tracking-[0.18em] text-slate-500">{{ $eyebrow }}</p>@endif<h2 class="{{ $eyebrow ? 'mt-1.5' : '' }} text-lg font-semibold tracking-tight text-white">{{ $title }}</h2></div>@if($action)<a href="{{ $action }}" class="shrink-0 text-sm font-semibold text-blue-300 transition hover:text-blue-200">View all →</a>@endif</div><div class="p-5 sm:p-6">{{ $slot }}</div></section>
